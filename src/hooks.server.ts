import type { Handle } from '@sveltejs/kit';
// import { auth } from '$lib/auth/utils';

export { handle } from "$lib/auth/utils";

// export const handle: Handle = async ({ event, resolve }) => {
//     return auth.handle({ event, resolve });
// };